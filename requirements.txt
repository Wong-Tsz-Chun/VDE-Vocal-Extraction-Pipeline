# Requirements for main environment (VDE_Proj\env)
# This environment runs RoFormer models and orchestrates the pipeline

# Audio Processing
audio-separator[gpu]>=0.19.0
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0
pydub>=0.25.0

# Deep Learning (CUDA 11.x compatible)
torch>=2.0.0
torchvision>=0.15.0
demucs>=4.0.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
