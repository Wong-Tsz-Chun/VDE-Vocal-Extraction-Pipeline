# Requirements for cuda12_env (CUDA 12 environment for ONNX models)
# This is a separate conda environment for ONNX acceleration

# Install PyTorch with CUDA 12.1 first:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ONNX Runtime with CUDA 12 support
onnxruntime-gpu>=1.17.0

# Audio Separator with GPU support
audio-separator[gpu]>=0.40.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
tqdm>=4.65.0
pydub>=0.25.0
